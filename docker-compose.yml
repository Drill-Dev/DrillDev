version: "2.4"
services:
  backend:
    tty: true
    init: true
    build:
      context: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/root/app
      - backend_venv:/root/app/.venv

volumes:
  backend_venv: